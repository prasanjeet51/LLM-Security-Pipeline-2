- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    pip install -r requirements.txt
    # Add any extra testing/type checking dependencies here
    pip install mypy pandas datasets pandera python-json-logger

- name: Run mypy
  run: mypy src/
